import 'package:flutter/material.dart';

class PersonList extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('listas de testes'),
    );
  }
}
